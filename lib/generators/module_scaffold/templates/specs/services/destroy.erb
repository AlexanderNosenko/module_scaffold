require 'rails_helper'

describe <%= @services_helper.full_class_name('destroy') %> do
  subject { described_class.new(<%= @services_helper.resource_name %>).call }

  let_it_be(:<%= @services_helper.resource_name %>) { create(:<%= @services_helper.resource_name %>) }

  describe '#call' do
    it 'deletes <%= @services_helper.resource_name %>' do
      expect { subject }.to change { <%= @services_helper.module_full_name %>.count }.by(-1)
    end
  end
end
