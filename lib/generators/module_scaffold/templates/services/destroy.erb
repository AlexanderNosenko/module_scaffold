<%= mc_wrap_with_namespaces(@services_helper.namespace_dirs) do
  <<-EOS
class #{@services_helper.class_name('destroy')} < ApplicationService

  def initialize(#{@services_helper.resource_name})
    @#{@services_helper.resource_name} = #{@services_helper.resource_name}
  end

  def call
    #{@services_helper.resource_name}.destroy!
    success(#{@services_helper.resource_name})
  rescue ActiveRecord::RecordNotDestroyed => e
    failure(e.record)
  end

  private

  attr_reader :params, :#{@services_helper.resource_name}

end
EOS
end %>
